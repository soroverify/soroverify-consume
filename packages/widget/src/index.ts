/**
 * @soroverify/widget — embeddable <soroverify-badge> source-verification badge.
 *
 * Importing this module registers the custom element. The React wrapper is a
 * separate entry point (./react).
 */
export {
  SOROVERIFY_BADGE_TAG,
  SoroverifyBadgeElement,
  STALENESS_THRESHOLD_MS,
} from './element.js';
